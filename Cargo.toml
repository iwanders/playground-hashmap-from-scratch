[package]
name = "hashmap_from_scratch"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"

[dependencies]


[dev-dependencies]
rand = "0.8.5"
criterion = { version = "0.5", features = ["html_reports"] }


[[bench]]
name = "bench"
harness = false
